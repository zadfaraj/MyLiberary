<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bb3"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:context=".Nazarat">

    <EditText
        android:id="@+id/act_nazarat_payam"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="10dp"
        android:layout_marginTop="50dp"
        android:layout_weight="2"
        android:background="@drawable/paperbackground"
        android:ems="10"
        android:gravity="top"
        android:hint="@string/matne_payam"
        android:inputType="textMultiLine"
        android:maxLength="500"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:textColor="@color/black"
        android:textDirection="rtl"
        android:textSize="20sp" />

    <Button
        android:id="@+id/button10"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="50dp"
        android:background="#00FFFFFF"
        android:onClick="btn_ersale_payam"
        android:text="@string/ersale_payam"
        android:textSize="20dp"
        android:textStyle="bold" />
</LinearLayout>